import React from 'react';
import TextInput from './DevextremeTextInput';
import renderer from 'react-test-renderer';

describe('DevextremeTextInput', () => {
   it('renders without error', () => {
      
   });
});
